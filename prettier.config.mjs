/** @type {import("prettier").Config} */
const config = {
  bracketSpacing: true,
  semi: false,
  trailingComma: "all",
  printWidth: 80,
  tabWidth: 2,
  singleQuote: false,
};

export default config;
